import { BifrostNode } from '../class/client.js';
import type { SignedMessage } from '@cmdcode/nostr-p2p';
import type { ApiResponse, SignSessionPackage, SignRequestConfig, SignatureEntry, SighashVector } from '../types/index.js';
export declare function sign_handler_api(node: BifrostNode, msg: SignedMessage<SignSessionPackage>): Promise<void>;
export declare function sign_queue_api(node: BifrostNode): (message: string | string[]) => Promise<SignatureEntry>;
export declare function sign_request_api(node: BifrostNode): (message: string | SighashVector[], options?: Partial<SignRequestConfig>) => Promise<ApiResponse<SignatureEntry[]>>;
