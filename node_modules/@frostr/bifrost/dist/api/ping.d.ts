import { BifrostNode } from '../class/client.js';
import type { SignedMessage } from '@cmdcode/nostr-p2p';
import type { ApiResponse, PeerPolicy } from '../types/index.js';
export declare function ping_handler_api(node: BifrostNode, msg: SignedMessage<string>): Promise<void>;
export declare function ping_request_api(node: BifrostNode): (pubkey: string) => Promise<ApiResponse<PeerPolicy>>;
