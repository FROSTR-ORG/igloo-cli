import { BifrostNode } from '../class/client.js';
import type { SighashVector, SignatureEntry } from '../types/index.js';
export declare class SignerQueue {
    private readonly _ival;
    private readonly _node;
    private _queue;
    private _timer;
    constructor(node: BifrostNode);
    get node(): BifrostNode;
    get timer(): NodeJS.Timeout | null;
    push(sigvec: SighashVector): Promise<SignatureEntry>;
    process(): Promise<void>;
    schedule(): void;
}
