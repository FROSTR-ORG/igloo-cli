import type { SignerConfig, ECDHPackage, GroupPackage, SignSessionPackage, SharePackage, PartialSigPackage } from '../types/index.js';
export declare class BifrostSigner {
    private readonly _config;
    private readonly _group;
    private readonly _share;
    private readonly _pubkey;
    constructor(group: GroupPackage, share: SharePackage, options?: Partial<SignerConfig>);
    get config(): SignerConfig;
    get group(): GroupPackage;
    get pubkey(): string;
    gen_ecdh_share(members: number[], ecdh_pk: string): ECDHPackage;
    sign_message(message: string, auxrand?: string | Uint8Array): string;
    sign_session(session: SignSessionPackage): PartialSigPackage;
    unwrap(content: string, pubkey: string): string;
    wrap(content: string, pubkey: string): string;
}
