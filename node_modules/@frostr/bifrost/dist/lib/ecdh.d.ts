import type { SecretShare } from '@cmdcode/frost';
import type { ECDHPackage } from '../types/index.js';
export declare function create_ecdh_pkg(members: number[], ecdh_pk: string, secshare: SecretShare): {
    idx: number;
    keyshare: string;
    members: number[];
    ecdh_pk: string;
};
export declare function combine_ecdh_pkgs(pkgs: ECDHPackage[]): string;
