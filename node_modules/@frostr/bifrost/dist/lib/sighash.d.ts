import type { SharePackage, SighashVector, SighashShare, CommitPackage, SighashCommit } from '../types/index.js';
export declare function format_sigvector(message: string | string[]): SighashVector;
export declare function create_sighash_commit(session_id: string, commit: CommitPackage, sigvec: SighashVector): SighashCommit;
export declare function create_sighash_share(session_id: string, share: SharePackage, sigvec: SighashVector): SighashShare;
export declare function get_sighash_binder(session_id: string, member_idx: number, sighash: SighashVector): string;
