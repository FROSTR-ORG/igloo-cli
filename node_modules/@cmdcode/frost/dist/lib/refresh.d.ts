import { Bytes } from '@cmdcode/buff';
import type { SecretShare, SecretSharePackage } from '../types/index.js';
export declare function gen_refresh_shares(index: number, threshold: number, share_max: number, secrets?: Bytes[]): SecretSharePackage;
export declare function refresh_share(refresh_shares: SecretShare[], current_share: SecretShare): SecretShare;
