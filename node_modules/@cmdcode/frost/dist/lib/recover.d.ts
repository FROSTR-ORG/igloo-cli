import type { SecretShare, SecretSharePackage } from '../types/index.js';
export declare function gen_recovery_shares(members: number[], share: SecretShare, target: number, threshold: number, secrets?: string[]): SecretSharePackage;
export declare function recover_share(shares: SecretShare[], idx: number): SecretShare;
export declare function verify_recovery_share(): void;
