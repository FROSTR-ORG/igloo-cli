import { Bytes } from '@cmdcode/buff';
export declare namespace Assert {
    function ok(value: unknown, message?: string): asserts value;
    function exists<T>(input?: T | null, err_msg?: string): asserts input is NonNullable<T>;
    function size(input: Bytes, size: number, err_msg?: string): boolean;
}
