import type { MessageData, MessageTemplate, SignedEvent, SignedMessage } from '../types/index.js';
export declare function decrypt_payload(event: SignedEvent, seckey: string): string;
export declare function finalize_message(template: MessageTemplate): MessageData;
export declare function create_payload(tag: string, data: string, id: string): string;
export declare function parse_envelope(envelope: string, event: SignedEvent): SignedMessage;
