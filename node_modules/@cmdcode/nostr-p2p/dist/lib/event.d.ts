import type { EventConfig, EventTemplate, SignedEvent } from '../types/index.js';
export declare function create_event(config: EventConfig, payload: string, peer_pk: string, seckey: string): SignedEvent;
export declare function sign_event(seckey: string, template: EventTemplate): SignedEvent;
export declare function verify_event(event: SignedEvent): string | null;
